{ 
    "version": 2,
    
    "builds": [
        {
            "src": "./UserServer/server.js",
            "use": "@vercel/node" 
        }
    ],

    "routes":[
        {
            "src":"/(.*)",
            "dest": "/UserServer"
        }
    ]
}